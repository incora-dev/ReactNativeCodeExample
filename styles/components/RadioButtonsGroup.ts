import { StyleSheet } from 'react-native';

const RadioButtonsGroupStyles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginBottom: 15,
  },
});

export default RadioButtonsGroupStyles;
